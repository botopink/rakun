//// rakun-session — sessions (front 18): the immutable `Session` value, the
//// `SessionStore` behavior and the `SessionRepository` handle, three arms
//// (ETS default, SQL over rakun-data, Redis), 32-byte ids, HMAC-signed cookies
//// verified in constant time, rotation, the chain entry at −350 and the
//// `sessions` endpoint with the `session` health indicator.
////
//// Order matters: a module is listed after every sibling it imports.

pub mod host;
pub mod session;
pub mod signing;
pub mod session_config;
pub mod session_cookie;
pub mod store_ets;
pub mod store_sql;
pub mod store_redis;
pub mod session_filter;
pub mod session_endpoint;
