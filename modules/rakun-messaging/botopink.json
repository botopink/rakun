{
  "name": "rakun-messaging",
  "version": "0.0.1",
  "description": "Rakun module — the listener registry and dispatch loop, AMQP + Kafka + RabbitMQ Streams + JMS arms, the publisher, retry / dead-letter / idempotency — erlang",
  "src": "src/",
  "entry": "root.bp",
  "target": "erlang",
  "targets": ["erlang"],
  "files": ["root.bp"],
  "dependencies": {
    "rakun": {
      "workspace": true
    }
  }
}
