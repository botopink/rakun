//// rakun-logging — logger, levels and groups, structured formats, file output
//// and rotation, correlation id, error digest, the `loggers` and `logfile`
//// endpoints. Front 17. erlang only; the host module is
//// `src/sidecars/rakun_logging.erl` (OTP `logger`).
////
//// The tree is in dependency order, the same order as `botopink.json`'s
//// `files`: a module is listed after every sibling it imports.

pub mod cells;
pub mod levels;
pub mod formats;
pub mod correlation;
pub mod logging;
pub mod digest;
pub mod setup;
pub mod endpoints;
