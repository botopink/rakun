//// rakun-hateoas — HAL resources for rakun (front 21): `Link`, the link set,
//// `#[halResource]`, collections, `linkTo` over the route table and the
//// `application/hal+json` response. Reached via `import {…} from "rakun-hateoas"`.

pub mod hal;
