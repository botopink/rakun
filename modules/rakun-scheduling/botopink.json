{
  "name": "rakun-scheduling",
  "version": "0.0.1",
  "description": "Rakun module — #[scheduler] with #[scheduled]/#[fixedRate]/#[fixedDelay], the six-field cron parser, the supervised executor, the scheduledtasks endpoint and the scheduling health indicator (front 16); the durable job store is front 84's — erlang",
  "src": "src/",
  "entry": "root.bp",
  "target": "erlang",
  "targets": ["erlang"],
  "files": ["root.bp", "cron.bp", "registry.bp", "markers.bp", "executor.bp", "endpoint.bp"],
  "dependencies": {
    "rakun": {
      "workspace": true
    },
    "rakun-web": {
      "workspace": true
    },
    "rakun-actuator-api": {
      "workspace": true
    },
    "rakun-actuator": {
      "workspace": true
    }
  }
}
