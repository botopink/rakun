{
  "name": "rakun-security",
  "version": "0.0.1",
  "description": "Rakun module — the security context (Principal, Authentication), the path policy, the chain entry at order -300, HS256 JWT and Basic authentication, the PBKDF2 password encoder, UserDetailsService (in-memory and SQL arms), method security (#[methodSecurity] -> <Type>Sec proxy) and CSRF; OAuth2 client, OIDC login, SAML2, LDAP follow (front 79) — erlang",
  "src": "src/",
  "entry": "root.bp",
  "target": "erlang",
  "targets": [
    "erlang"
  ],
  "files": [
    "root.bp",
    "principal.bp",
    "policy.bp",
    "jwt.bp",
    "password.bp",
    "users.bp",
    "users_sql.bp",
    "basic.bp",
    "csrf.bp",
    "method_security.bp",
    "security_filter.bp",
    "security.bp"
  ],
  "dependencies": {
    "rakun": {
      "workspace": true
    },
    "rakun-actuator-api": {
      "workspace": true
    },
    "rakun-web": {
      "workspace": true
    },
    "rakun-data": {
      "workspace": true
    }
  }
}
