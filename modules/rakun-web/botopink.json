{
  "name": "rakun-web",
  "version": "0.0.1",
  "description": "Rakun module — the filter chain (one ordered list between the socket and the route handler), its two entry points #[filter] and #[middleware], Next, withHeader, CORS and RFC 9457 problem details — erlang",
  "src": "src/",
  "entry": "root.bp",
  "target": "erlang",
  "targets": [
    "erlang"
  ],
  "files": [
    "root.bp",
    "filter.bp",
    "error.bp",
    "middleware.bp",
    "cors.bp",
    "convention.bp"
  ],
  "dependencies": {
    "rakun": {
      "workspace": true
    }
  }
}
