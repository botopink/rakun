{
  "name": "rakun-actuator-api",
  "version": "0.0.1",
  "description": "Rakun module — the actuator contract every other module registers into: Health, HealthIndicator, InfoContributor, Endpoint, EndpointResponse, Span; #[healthIndicator], #[infoContributor], #[endpoint], #[instrumentation]; the registration cells and the span emitter. Decides nothing — erlang",
  "src": "src/",
  "entry": "root.bp",
  "target": "erlang",
  "targets": ["erlang"],
  "files": ["root.bp", "contract.bp", "registration.bp", "span.bp"],
  "dependencies": {
    "rakun": {
      "workspace": true
    }
  }
}
