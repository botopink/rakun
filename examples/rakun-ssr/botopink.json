{
  "name": "rakun-ssr-example",
  "version": "0.0.1",
  "description": "rakun SSR showcase — a file-convention page wrapped in its layouts, rendered through the escaping walker, wrapped in a document with the payload a client reconnects to; reached through `from \"rakun\"` as a real consumer",
  "src": "src/",
  "entry": "main.bp",
  "target": "commonJS",
  "targets": ["commonJS"],
  "dependencies": {
    "rakun": {
      "workspace": true
    }
  }
}
