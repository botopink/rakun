{
  "name": "rakun-ssr-example",
  "version": "0.0.1",
  "description": "rakun page-path showcase — pages registered through `page(pattern, render)`, renderers writing plain text through the ChunkWriter, the page path as the router fallback; reached through `from \"rakun-app\"` as a real consumer (rakun builds no HTML, decision 113)",
  "src": "src/",
  "entry": "main.bp",
  "target": "erlang",
  "targets": ["erlang"],
  "dependencies": {
    "rakun": {
      "workspace": true
    },
    "rakun-app": {
      "workspace": true
    }
  }
}
