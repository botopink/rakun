# Per-platform CI for rakun.
#
# Bootstrap path: check out this lib + botopink-lang, place this lib under
# `botopink-lang/repository/rakun/` so the multi-root resolver finds it
# without extra config, then run `zig build test-libs -- --lib rakun
# --target <matrix.target>`.
#
# `BOTOPINK_LANG_REF` (repo variable, default `feat`) lets the lib pin a
# specific botopink-lang SHA during a compiler-version dance.
name: test

on:
  push:
    branches: [feat, master, main]
  pull_request:
    branches: [feat, master, main]

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

env:
  LIB_NAME: rakun
  BOTOPINK_LANG_REPO: botopink/botopink-lang

jobs:
  test:
    name: ${{ matrix.runner }} · ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        # `allow_fail: true` on the `erlang` axis: every `rk*` host cell in
        # src/runtime.bp carries only an `@External.Node` form, so an erlang
        # run stops at `function rkScan/1 undefined` (measured 2026-09-18 —
        # only http.bp's 1 test, which touches no host cell, passes). Unlike
        # emilia's and onze's process-dictionary cells, rakun's DI graph,
        # router and HTTP server are 231 lines of runtime.mjs that do not fit
        # an inline `@External.Erlang` expression; the axis stays soft until a
        # library can ship an `.erl` host module (the CLI ships only `.mjs`
        # sidecars). windows-2022 commonJS now runs the source step under
        # `shell: bash` (ci-tail W1), closing the PowerShell `${LIB_NAME}`
        # expansion gap.
        include:
          - { runner: ubuntu-22.04, target: commonJS, allow_fail: false }
          - { runner: ubuntu-22.04, target: erlang,   allow_fail: true  }
          - { runner: ubuntu-22.04, target: beam,     allow_fail: false }
          - { runner: macos-14,     target: commonJS, allow_fail: false }
          - { runner: macos-14,     target: erlang,   allow_fail: true  }
          - { runner: macos-14,     target: beam,     allow_fail: false }
          - { runner: windows-2022, target: commonJS, allow_fail: false }
    runs-on: ${{ matrix.runner }}
    continue-on-error: ${{ matrix.allow_fail }}
    steps:
      - name: Checkout this lib
        uses: actions/checkout@v5
        with:
          path: self

      - name: Checkout botopink-lang
        uses: actions/checkout@v5
        with:
          repository: ${{ env.BOTOPINK_LANG_REPO }}
          ref: ${{ vars.BOTOPINK_LANG_REF || 'feat' }}
          path: botopink-lang

      - name: Place lib under botopink-lang/repository/${{ env.LIB_NAME }}/
        shell: bash
        run: |
          mkdir -p botopink-lang/repository
          # rsync may not be available on the windows runner — fall back to
          # cp -R; both produce a real tree (no symlink follow-through quirks).
          if command -v rsync >/dev/null 2>&1; then
            rsync -a --delete self/ "botopink-lang/repository/${LIB_NAME}/"
          else
            rm -rf "botopink-lang/repository/${LIB_NAME}"
            cp -R self "botopink-lang/repository/${LIB_NAME}"
          fi

      - name: Install required runtimes (linux)
        if: matrix.runner == 'ubuntu-22.04' && (matrix.target == 'erlang' || matrix.target == 'beam')
        run: |
          sudo apt-get update -y
          sudo apt-get install -y erlang

      - name: Install required runtimes (macos)
        if: matrix.runner == 'macos-14' && (matrix.target == 'erlang' || matrix.target == 'beam')
        run: |
          brew install erlang

      - name: Install Node 20 (for commonJS target)
        if: matrix.target == 'commonJS'
        uses: actions/setup-node@v5
        with:
          node-version: '20'

      # H7 (v0.beta.19) — release fast path. Opt-in via the lib's
      # `BOTOPINK_USE_RELEASE_FASTPATH` repo variable. Set to `true` after
      # botopink-lang has published at least one tagged release; until then,
      # the source path below is the default.
      - name: Install botopink (release fast path)
        if: ${{ vars.BOTOPINK_USE_RELEASE_FASTPATH == 'true' }}
        shell: bash
        run: |
          if [ "${{ matrix.runner }}" = "windows-2022" ]; then
            powershell -ExecutionPolicy Bypass -Command "iwr https://botopink.dev/install.ps1 -UseBasicParsing | iex"
            echo "$USERPROFILE\\.bpmp\\bin" >> $GITHUB_PATH
          else
            curl --proto '=https' --tlsv1.2 -sSfL https://botopink.dev/install.sh | sh
            echo "$HOME/.bpmp/bin" >> $GITHUB_PATH
          fi

      - name: Install Zig (source build path)
        if: ${{ vars.BOTOPINK_USE_RELEASE_FASTPATH != 'true' }}
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Print build identity
        shell: bash
        run: |
          echo "lib:     ${LIB_NAME}"
          echo "target:  ${{ matrix.target }}"
          echo "runner:  ${{ matrix.runner }}"
          echo "fastpath: ${{ vars.BOTOPINK_USE_RELEASE_FASTPATH }}"
          echo "ref:     ${{ vars.BOTOPINK_LANG_REF || 'feat' }}"

      - name: zig build install (source build path)
        if: ${{ vars.BOTOPINK_USE_RELEASE_FASTPATH != 'true' }}
        working-directory: botopink-lang
        run: zig build install

      - name: zig build test-libs --lib ${{ env.LIB_NAME }} --target ${{ matrix.target }} (source)
        if: ${{ vars.BOTOPINK_USE_RELEASE_FASTPATH != 'true' }}
        working-directory: botopink-lang
        shell: bash
        run: zig build test-libs -- --lib "${LIB_NAME}" --target "${{ matrix.target }}"

      - name: botopink-lib-test --lib ${{ env.LIB_NAME }} --target ${{ matrix.target }} (fast path)
        if: ${{ vars.BOTOPINK_USE_RELEASE_FASTPATH == 'true' }}
        working-directory: botopink-lang
        shell: bash
        run: botopink-lib-test --lib "${LIB_NAME}" --target "${{ matrix.target }}"

      # Every examples/*/ with a botopink.json builds (runExamplesGate, the same
      # stage as the pre-commit hook); scripts/known-broken-examples.txt lists
      # the ones allowed to fail. Each example uses its own manifest target, so
      # this runs once, not per target.
      - name: Examples build
        if: matrix.target == 'commonJS'
        working-directory: botopink-lang/repository/${{ env.LIB_NAME }}
        shell: bash
        run: |
          source scripts/git-hooks/lib/runner-standalone.sh
          bin=$(locateBotopink) || { echo "botopink binary not found"; exit 1; }
          runExamplesGate "$bin"
